// graphql-schema/schema.graphql
type Query {
  getOrderById(id: String): [Order]
}

type Order {
  id: String
  date: AWSDateTime
  totalAmount: Float
  customer: Customer
  products: [Product]
}

type Customer {
  email: String
  fullName: String
}

type Product {
  price: Float
  quantity: Int
}